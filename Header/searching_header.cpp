// If the name is enclosed by angle brackets, <....>
// it will search the name in the include folder only.
#include<iostream>

// Else, it will search the name in your working directory.
// Since we don't have iostream.h, this code will obviously throw error.
#include "iostream.h"


int main() {
  
  return 0;
}